"""SwingAI backend — Phase 0: the validation harness (quant brain + backtest).

The picker is deterministic math (never an LLM) so it can be backtested and shown
transparently. See blueprint/04-picker-strategy.md and blueprint/05-validation-backtest.md.
"""

__version__ = "0.0.1"
